Embed the social button array <%=render 'layouts/social_section', :title => article.title, :url => request.protocol + request.host_with_port + request.fullpath %> into your rails app page.
<%= link_to article_path(article) do %>
	Read More <i class="icon-signin"></i>
<% end %>