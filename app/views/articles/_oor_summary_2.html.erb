Store global constants in initializer to access them everywhere in your code.
<%= link_to article_path(article) do %>
	Read More <i class="icon-signin"></i>
<% end %>
<% code = "class Constant
  def self.gender
    {:female => 'gal', :male => 'guy', :else => '*@#\$'}
  end
end" %>
<%= render 'code_section', :title => "/config/initializers/constants.rb", :code => code, :lang => :rb %>