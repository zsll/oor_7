<p>
	Recently I installed the <%= link_to "http://rubygems.org/gems/pg" do%>pg<% end %> gem to use <%= link_to "www.postgresql.org/" do %>Postgres<% end %>. Later when I try to start rails console, I got the weird error message below.
	<% code_content = "$ rails c
/usr/local/rvm/gems/ruby-1.9.3-p194/gems/pg-0.15.1/lib/pg_ext.bundle: [BUG] Segmentation fault
ruby 1.8.7 (2012-02-08 patchlevel 358) [universal-darwin12.0]

Abort trap: 6"%>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	One <%= link_to "stackoverflow", "http://stackoverflow.com/questions/8958998/segmentation-fault-with-ruby-1-8-7"%> thread suggests reinstalling the gem.
	<% code_content = "$ gem uninstall pg
$ gem install pg"%>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	On the same thread, another claimed success by prepending <code>bundle exec</code>.
	<% code_content = "$ bundle exec rails c"%>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	I tried both while ended up in failure. So I tried to rebuild all gems.
	<% code_content = "$ gem pristine --all" %>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	This takes quite a while and still doesn't help. Finally I did the most intuitive fix.
	<% code_content = "$ rvm use 1.9.3"%>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	Hooray! Rails comes back to life.
	<% code_content = "$ rails c
Loading development environment (Rails 3.2.13)
1.9.3p194 :001 >"%>
	<%= render 'code_section', :title => "terminal", :code => code_content %>
	Somehow when I opened a new terminal tab/window, I have to repeat this fix. 
</p>
